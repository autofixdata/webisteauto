# Dev tools (not uploaded to production)

Scripts here are for local maintenance only. They are excluded from `npm run pack:deploy`.

- `generate-car-db.js` — rebuilds `src/lib/largeCarDatabase.json` from source data

Dictionary update scripts were removed from the repo root (legacy one-off files with hardcoded paths).
